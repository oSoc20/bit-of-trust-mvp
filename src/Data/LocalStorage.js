
	let stroe = window.localStorage;


	console.info("My Cat", myStorage.getItem("myCat"));


const getData = (str = "") => {
  return true;
}


const setAliasContact = (tokenStr = "", alias = "") => {

  if(tokenStr && alias && tokenStr.length > 0 && alias.length > 0){
    setTokenAlias(tokenStr, alias);
  }
  else {
    // throw error of zo
  }
}

const setTokenAlias = (token, alias) => {

  if(store)

};
